# GENESIS v4 Production Environment Variables
#
# These are automatically created from GitHub Secrets during deployment.
# Required GitHub Secrets (Settings > Secrets and variables > Actions):
#
#   SERVER_HOST         - VPS IP address or hostname
#   SERVER_USER         - SSH username (e.g., root)
#   SERVER_SSH_KEY      - Private SSH key for VPS access
#   POSTGRES_PASSWORD   - PostgreSQL password (required)
#   REDIS_PASSWORD      - Redis password (required)
#   SECRET_KEY          - JWT secret key, 256-bit (required)
#   TWITTER_CLIENT_ID   - Twitter OAuth client ID
#   TWITTER_CLIENT_SECRET - Twitter OAuth client secret
#   ACME_EMAIL          - Email for Let's Encrypt SSL
#   TRAEFIK_USERS       - Traefik dashboard auth (htpasswd format)
#   CLAUDE_API_KEY      - Claude API key (optional)

# ============ Database ============
POSTGRES_USER=genesis
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=genesis

# ============ Redis ============
REDIS_PASSWORD=your-redis-password-here

# ============ Security ============
SECRET_KEY=your-256-bit-secret-key-here

# ============ Twitter OAuth ============
TWITTER_CLIENT_ID=your-twitter-client-id
TWITTER_CLIENT_SECRET=your-twitter-client-secret

# ============ AI (Optional) ============
CLAUDE_API_KEY=your-claude-api-key

# ============ SSL/HTTPS ============
ACME_EMAIL=your-email@example.com

# ============ Traefik Dashboard Auth ============
# Generate with: htpasswd -nb admin your-password
TRAEFIK_USERS=admin:$$apr1$$...
